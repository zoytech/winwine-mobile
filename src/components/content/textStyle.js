import {Text, View} from "react-native";

const useTextStyle = {
    textContainer: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
    text: {
        fontWeight: 'bold',
    }
}
export default useTextStyle;
